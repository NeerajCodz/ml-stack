# Alerts and diagnostics

Alert on NaN/Inf, divergence, stalled heartbeat, missing metrics, threshold regression, repeated failures, orphaned resources, and budget breach. Include severity, first/last event, metric window, evidence path, likely cause, bounded operator action, and whether retry is approved. Never silently alter a run.
