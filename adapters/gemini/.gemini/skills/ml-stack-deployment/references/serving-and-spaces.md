# Serving targets

For a local service, endpoint, or Space define request/response schema, batching, limits, timeout, auth, secret references, health/readiness, observability, scaling, region, and cost. Treat HF Spaces and endpoint changes as external writes. If unavailable at runtime, hand exact deployment intent to `ml-stack-hub`.
