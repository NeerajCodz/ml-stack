# Robustness and calibration

Probe distribution shift, perturbations, long inputs, adversarial or malformed inputs, seed variance, calibration, abstention, latency, and resource pressure as appropriate. Record test generation, severity, observed failures, and threshold. Treat robustness gaps as release blockers when the use case requires them.
