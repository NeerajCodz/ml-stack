# Method selection

Choose SFT for supervised behavior, DPO for preference pairs, GRPO for verifiable or reward-driven generation, reward modeling for learned preference scores, sentence-transformer objectives for embedding similarity, and vision methods by annotation contract. State why the method fits, what it cannot establish, and which evaluation owns the gate.
