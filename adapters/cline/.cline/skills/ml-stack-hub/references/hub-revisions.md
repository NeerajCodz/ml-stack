# Hub revisions

Resolve exact model, dataset, Space, or repository URLs and immutable commits/tags before reading or proposing changes. Record branch context, file manifest, card revision, content hashes, and retrieval timestamp. Mutable main is a pointer, not reproducibility evidence.
