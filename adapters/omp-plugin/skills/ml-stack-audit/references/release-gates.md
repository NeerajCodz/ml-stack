# Release gates

Require locked requirements, clean data/split evidence, eligible validation, reproducible environment, passing thresholds and required slices, complete artifacts/hashes, privacy/license clearance, model card, target security, rollback, and explicit approval. Missing evidence yields `ABSTAIN`; a violated gate yields `FAIL`.
