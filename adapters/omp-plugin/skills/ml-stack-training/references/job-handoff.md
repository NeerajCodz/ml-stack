# Training job handoff

Specify immutable inputs, environment lock, command or entrypoint, resource profile, secrets by name, outputs, logging, checkpoint cadence, evaluation cadence, stop rules, budget ceiling, and resume key. Mark submission unavailable unless the runtime exposes and reports a matching operation. Hand off to `ml-stack-compute`.
