# Metric handoff

Hand off run ID, event range, metric definitions, baseline, slices, uncertainty, alerts, artifact hashes, and retrieval path. `ml_stack.events` can replay repository ledger events; it is not proof of an external provider dashboard. Send comparison to `ml-stack-experiment`, quality to `ml-stack-evaluation`, and release evidence to `ml-stack-audit`.
