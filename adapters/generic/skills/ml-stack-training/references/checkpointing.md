# Checkpointing and persistence

Define save cadence, retention count, optimizer and scheduler state, tokenizer/processor, training config, data/model revisions, artifact hashes, and best-checkpoint rule. Trackio or Hub persistence remains a plan unless an adapter is observed. Resume only from a verified checkpoint with compatible environment and validation.
