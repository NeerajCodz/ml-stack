# Benchmark matrix

Map task to benchmark, dataset revision, backend, model revision, prompt or preprocessing, metrics, slices, threshold, and budget. Include a representative smoke case and an approved scale matrix. Separate public benchmark claims from private acceptance tests and record licenses.
