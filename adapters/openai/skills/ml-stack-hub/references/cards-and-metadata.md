# Cards and metadata

Inspect README/model card, dataset card, YAML metadata, license, languages, tasks, tags, datasets, base model, files, and intended use. Separate provider-reported metadata from verified measurements. Missing or conflicting license and provenance blocks publication and goes to `ml-stack-audit`.
