# Result comparison

Compare only eligible runs with matching target, validation, metric definition, data and model compatibility, and resource accounting. Report point estimates, uncertainty, slices, failures, cost, and artifact links. The experiment can recommend a candidate but `ml-stack-audit` independently decides release readiness.
