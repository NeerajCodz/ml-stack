# Vision training

Classification needs class map and stratified splits. Detection needs image dimensions, box format, coordinate bounds, and empty-image policy. Segmentation needs mask encoding, palette/class IDs, and resize policy. Record augmentations, pretrained revision, processor, resolution, metrics, and visualization checks before training.
