# Reproducibility record

Capture code commit, dependency lock or PEP 723/UV environment, hardware, drivers, model and data revisions, seeds, commands, configuration, run directory, timestamps, resource use, logs, and artifact hashes. Record nondeterminism sources and rerun variance. Never call a result reproducible when a required input is unknown.
