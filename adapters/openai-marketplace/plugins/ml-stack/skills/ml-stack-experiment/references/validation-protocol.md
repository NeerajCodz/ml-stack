# Validation protocol

Pin dataset revision, split files, folds, temporal cutoff, groups, slices, preprocessing, acceptance set, metrics, and random seeds before execution. Do not tune on held-out acceptance data. Record exclusions and leakage findings. A changed validation revision makes runs ineligible for direct comparison.
