# Cost controls

Set hard limits for runtime, samples/tokens, retries, concurrency, storage, and spend. Show estimated and worst-case cost, approval scope, alert thresholds, and shutdown action. Paid or remote work remains blocked until approval; a failed request is not permission to retry elsewhere.
