# Leakage checklist

Check exact and near duplicates, shared users/groups, temporal overlap, target-derived fields, post-outcome features, prompt/completion contamination, benchmark examples, preprocessing fitted across splits, augmentation bleed, and label metadata. Record detector, threshold, affected IDs/counts, remediation, and whether validation revision changes. Never silently remove evidence.
