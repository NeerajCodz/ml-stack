# SFT, DPO, and GRPO preflight

Validate message roles and loss masks for SFT; prompt/chosen/rejected alignment and reference policy for DPO; prompt, reward function, sampling limits, and exploit checks for GRPO. Pin tokenizer, max length, beta or reward settings, seeds, and held-out evaluation. Missing format or reward semantics blocks execution.
