# Local models

Record local path, exact model revision/hash, architecture, tokenizer/processor, quantization, context/image limits, runtime, device map, and OpenAI-compatible API prefix. A local prefix is routing syntax, not model identity. Verify loaded metadata and preserve the environment used for comparison.
