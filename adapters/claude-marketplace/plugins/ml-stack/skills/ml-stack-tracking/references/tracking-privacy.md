# Tracking privacy

Keep dashboards, logs, prompts, samples, traces, and artifacts private by default. Store secrets by reference, redact PII, define retention and access, and separate public metric summaries from raw data. Record visibility changes and approval. Unknown sensitivity blocks external tracking.
